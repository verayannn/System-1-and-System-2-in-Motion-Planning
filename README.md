# System-1-and-System-2-in-Motion-Planning

To install sofai (after we have created the venv for our package with the requiremnts)
-uv pip install sofai/.

Lunch:
python3 motion_planning_solver.py --s2 mpc --s1 neural --run_type s1 (this fails)
python3 motion_planning_solver.py --s2 cbf --s1 primitives --run_type sofai