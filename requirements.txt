# Top-level environment for the Dual Process Motion Planning experiments.
# This installs the local SOFAI package and the vendored safe_control package,
# plus the solver/runtime libraries required by the repo-level experiment scripts.

-e ./sofai
-e ./safe_control

# Core numerical stack
numpy==1.26.4
scipy>=1.15
matplotlib>=3.10

# Machine learning
torch>=2.2

# Optimization / control
casadi>=3.7
cvxpy>=1.7
osqp>=1.1
scs>=3.2
clarabel>=0.11
do-mpc[full]>=5.1.1 # optional
shapely>=2.0.7

# Plotting / utilities
pandas>=2.3
seaborn>=0.13
tqdm>=4.67

# Needed by vendored acados_template imports
deprecated>=1.2.14

# Optional, only if you use safe_control examples that require it
gurobipy
