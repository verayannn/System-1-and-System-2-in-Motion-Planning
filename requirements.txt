# Top-level environment for the Dual Process Motion Planning experiments.
# For new installs, prefer `uv sync --extra acados-template`.
# This file is kept for pip/conda users and mirrors the core project deps.

-e .

# Core numerical stack
numpy>=1.26.4,<2.0
scipy>=1.15
matplotlib>=3.10

# Machine learning
torch>=2.2

# Optimization / control
casadi>=3.7
cvxpy>=1.7
osqp>=1.1
scs>=3.2
clarabel>=0.11
do-mpc[full]>=5.1.1 # optional
shapely>=2.0.7

# Plotting / utilities
pandas>=2.3
seaborn>=0.13
tqdm>=4.67

# Needed by vendored acados_template imports
deprecated>=1.2.14
cython
setuptools-scm

# Optional, only if you use safe_control examples that require it:
# gurobipy
